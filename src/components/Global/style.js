import styled from 'styled-components'

export const StyleButtonSecondary = styled.a`
    background-color: transparent;
    color: var(--primary);
    border: 1px solid var(--primary);
    line-height: 4rem;
    padding: 1rem 4rem;
`